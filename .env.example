# API Secrets (Configure at least one to enable intelligent server-side generation)
GEMINI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# Optional: Force specific provider model ('groq', 'openrouter', or 'openai')
# If unset, the server will auto-detect based on the existence/prefix of available keys
AI_PROVIDER=
